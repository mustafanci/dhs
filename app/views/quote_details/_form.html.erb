<%= form_for(quote_detail) do |f| %>
  <% if quote_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote_detail.errors.count, "error") %> prohibited this quote_detail from being saved:</h2>

      <ul>
      <% quote_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.number_field :phone %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :county %>
    <%= f.text_field :county %>
  </div>

  <div class="field">
    <%= f.label :customer %>
    <%= f.check_box :customer %>
  </div>

  <div class="field">
    <%= f.label :service %>
    <%= f.check_box :service %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
